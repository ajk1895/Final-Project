//will pull info from form to display characters name

import React from "react";
import 'react-bootstrap';

//enter array and pull obj.name to create text 

export default function CharName({char}) {
    return (
        <>
            {char}
        </>
    )
}

